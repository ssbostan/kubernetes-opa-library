apiVersion: v1
kind: Service
metadata:
  name: test-opa-gatekeeper-block-service-loadbalancer
  labels:
    app.kubernetes.io/name: kubernetes-opa-library
    app.kubernetes.io/created-by: ssbostan
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
