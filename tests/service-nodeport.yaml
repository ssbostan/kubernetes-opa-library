apiVersion: v1
kind: Service
metadata:
  name: test-opa-gatekeeper-block-nodeport
  labels:
    app.kubernetes.io/name: kubernetes-opa-library
    app.kubernetes.io/created-by: ssbostan
spec:
  type: NodePort
  ports:
    - name: http
      port: 80
